/** Shared chrome so navbar, cards, and hero use the same red treatment. */
export const siteHeaderGradient =
  "bg-gradient-to-r from-[#1a0408] via-[#7a1520] to-[#2c060c]";

/** Vertical red treatment for cards and section backgrounds. */
export const siteHeaderGradientVertical =
  "bg-gradient-to-b from-[#1a0408] via-[#7a1520] to-[#2c060c]";

export const siteHeaderShadow =
  "shadow-[0_6px_28px_rgba(0,0,0,0.45),inset_0_1px_0_rgba(255,255,255,0.12)]";

/** Full lockup (hex + BULLS + subline). Add your file to /public with this name. */
export const homeHeroLogoSrc = "/logo-club-academy-hero.png";

/** White pill for CTAs on red (hero, header-matched cards, etc.). */
export const siteCtaOnRedClass =
  "inline-flex items-center justify-center rounded-xl bg-white px-6 py-3 font-semibold text-[#1a0408] shadow-sm transition hover:bg-white/90";

/** Red pill for CTAs on white / light sections. */
export const siteCtaOnLightClass =
  "inline-flex items-center justify-center rounded-xl bg-[#D91F26] px-6 py-3 font-semibold text-white transition hover:bg-[#b91a20]";
